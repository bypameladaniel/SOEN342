@startuml
participant "<u>:Console" as console
participant "<u>:UserInterface" as userInterface
participant "<u>:ClientDB" as clientDB
collections "<u>:Client" as client


[->console : enterTravelerDetails()
console->userInterface   : getNbOfClients()
userInterface-->console : Int nb

loop [[i= 0..nb]]
    opt ["existing client"]
        console->userInterface : promptClientLookup
        userInterface->clientDB : finClientByIDAndName(id, lastname)
        clientDB->client: get()
        client-->userInterface: Client c
        userInterface-->console : Client c
    end opt

    opt ["new client or no existing client"]
        console->userInterface : promptClientCreation
        userInterface->client : create(id, firstname, lastname, age)
        client-->userInterface: Client c
        userInterface-->console : Client c
    end opt

end loop

console->[ : List<Client> clients



@enduml